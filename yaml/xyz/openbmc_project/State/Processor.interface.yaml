description: >
    Implement to provide value of performance state of a processor (CPU, GPU, etc).

properties:
    - name: Value
      type: uint16
      description: >
          This is current discreate value of the sensor. 

    - name: ThermalTrip
      type: boolean
      description: >
        The system is in thermal trip.
      flags:
        - readonly