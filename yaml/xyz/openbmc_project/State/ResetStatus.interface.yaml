description: >
   Implement to provide Reset Requirements.

properties:
    - name: ResetRequired
      type: boolean
      description: >
            Represents Reset Requirement.
      flags:
          - readonly
    - name: DrainAndResetRequired
      type: boolean
      description: >
            Represents Drain and Reset Requirement.
      flags:
          - readonly
    - name: ResetAsserted
      type: boolean
      description: >
            Represents whether Reset is Asserted or not.
      flags:
          - readonly
